INSTALLATION STEPS:

The following modules are required:

argparse
cv2
numpy

Running the application:

python3 sheetmusic_detect.py -i path/to/image

Will display the five main staff lines for each group highlighted in red on the sheet music, and will display the total number of sheet music lines found.
